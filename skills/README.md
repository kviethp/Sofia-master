# Sofia Skills

Author source skills in Markdown with JSON frontmatter.

Use:

- `pnpm skills:validate`
- `pnpm skills:compile`
- `pnpm agent-system:conformance`

Compiled outputs are written to `.sofia/skills/` by default.
