# Model Policy

Sofia Master treats model selection as policy-controlled behavior.

Default priority:
1. GPT / Codex-oriented profiles
2. cheaper approved stable models
3. free models for low-risk, non-sensitive, non-blocking tasks only
